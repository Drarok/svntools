<?php
/**
 * Configuration for the svneligible command.
 */

return array(
	// Regular expressions to exclude from the paths.
	'exclude' => array(
		'branches' => array(),

		'releases' => array(
			// Exclude the 'current/' release.
			'#^current/?$#',
		),

		'tags' => array(),
	),
);