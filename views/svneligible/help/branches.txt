svneligible branches
	Display a two-level deep list from ^/branches, in natural sort order.

	If you have configured any 'branches' exclusions in config/svneligible.php,
	then those branches will be pruned from the list before being displayed.